import {NS} from "@ns";
function unlockCSEC(ns: NS) {
  // server: CSEC
}

function unlockNiteSec(ns: NS) {
  // server: avmnite-02h
}

export async function main(ns: NS) {
  // needs backdoors to work, cant do it rn.
}
